# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code (local-only, never upload — no Claude footprint on GitHub)
CLAUDE.md
.claude/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.env
venv/
.venv/

# GCP credentials (NEVER commit)
key.json
*.json.secret
credentials.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
key.json
key.json

# Large data files (do NOT commit)
week*/data/*.parquet
*.parquet

# Monitoring run outputs (uploaded as CI artifacts, not tracked)
week*/metrics-*.json

# Local-only scaffolding: symlinks that point data.py at the real Week 2 files
week2/data/processed/

# Report is submitted separately as a PDF, not tracked here
week3/REPORT.md

# Week 5 report source is local-only; the PDF is what gets submitted
week5/REPORT.md
week5/image/
